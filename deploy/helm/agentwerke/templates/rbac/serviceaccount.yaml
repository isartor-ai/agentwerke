apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agentwerke.fullname" . }}-worker
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agentwerke.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
